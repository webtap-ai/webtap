{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "startUrls": {
            "type": "array",
            "description": "Enter a link to a channel. You can also import a CSV file or Google Sheet with a list of URLs.\n",
            "example": [
                {
                    "url": "https://www.youtube.com/@Apify"
                }
            ]
        },
        "maxResults": {
            "type": "integer",
            "description": "Limit the number of regular videos (not streams, not shorts) you want to get from one channel. If you use several urls on the input you will get this number for each channel.",
            "example": 10
        },
        "maxResultsShorts": {
            "type": "integer",
            "description": "Limit the number of Shorts videos you want to get from the channel.",
            "example": 0
        },
        "maxResultStreams": {
            "type": "integer",
            "description": "Limit the number of Stream videos you want to get from the channel.",
            "example": 0
        },
        "sortVideosBy": {
            "type": "string",
            "description": "Maps to the three sorting buttons on the top of the channel videos page. Currently only applies to the main tab: Videos. Note that the order is approximate, due to the asynchronous nature of the scrap",
            "enum": [
                "NEWEST",
                "POPULAR",
                "OLDEST"
            ]
        }
    },
    "required": [
        "startUrls"
    ]
}